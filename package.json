{
  "scripts": {
    "postbuild": "node scripts/inject-keys.js",
    "package": "npm run build && npm run postbuild && electron-builder build"
  },
  "build": {
    "files": [
      "dist/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "dist/electron/config.js",
        "to": "config.js"
      }
    ]
  }
}